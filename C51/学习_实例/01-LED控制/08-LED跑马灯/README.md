# LED跑马灯

## 功能说明

使用C51库函数实现循环移位跑马灯。

## 硬件接线

| 引脚 | 连接 |
|:---|:---|
| P0.0 ~ P0.7 | 8个LED（低电平点亮） |

## 代码要点

```c
#include <intrins.h>

P0 = _cror_(P0, 1);  // 循环右移1位
P0 = _crol_(P0, 1);  // 循环左移1位
```

## 踩坑记录

- 需要 `#include <intrins.h>`
- `_crol_` / `_cror_` 是循环移位，不会丢位
- 比手动位运算更简洁
