# LCD1602万年历

> 普中开发板上的第一个完整实物项目，LCD1602 + DS1302 + DS18B20

[← 返回C52](../../README.md) · [← 返回首页](../../../README.md)

---

## 元数据

| 项目 | 说明 |
| :--- | :--- |
| 芯片 | STC89C52RC（普中开发板） |
| 工具链 | Keil uVision4（C51 / Keil4） |
| 仿真 | Proteus 7.8（仓库含 `proteus/基于LCD1602的万年历.DSN`） |
| 晶振 | 11.0592MHz（以开发板/工程为准） |
| 编码 | GBK / ANSI（`.c` 源码） |
| 验证状态 | **已实物验证**（并附仿真工程） |
| 预期现象 | 第一行 `年-月-日 星期`；第二行 `时:分:秒 温度℃`；K1 进设置，K2 切换项，K3/K4 加减 |
| 工程文件 | **不入库**（`.uvproj` 被 ignore）；本地若有 `c/wannianli.uvproj` 仅为本机文件 |

---

## 功能介绍

- LCD1602第一行显示：`2026-08-18 Mon`（年月日 + 星期）
- LCD1602第二行显示：`12:30:45 25.5C`（时间 + 温度）
- 4个按键设置时间：K1进入/退出设置，K2切换选项，K3加，K4减
- 支持闰年自动修正（2月天数自动判断）
- DS1302掉电走时（电池供电，时间不丢失）
- DS18B20温度采集（状态机非阻塞设计，每5秒更新一次）

## 🔌 硬件清单

| 物料 | 数量 | 说明 |
|:---|:---|:---|
| 普中开发板 | 1 | 主板（STC89C52） |
| LCD1602 | 1 | 液晶显示屏 |
| DS1302 | 1 | 实时时钟模块 |
| DS18B20 | 1 | 温度传感器 |
| 杜邦线 | 若干 | 连接用 |

### 引脚连接

| 模块 | 引脚 | 单片机 |
|:---|:---|:---|
| LCD1602 | RS | P2^6 |
| LCD1602 | RW | P2^5 |
| LCD1602 | EN | P2^7 |
| LCD1602 | D0~D7 | P0（需上拉电阻） |
| DS1302 | IO | P3^4 |
| DS1302 | RST | P3^5 |
| DS1302 | SCLK | P3^6 |
| DS18B20 | DQ | P3^7 |
| 按键 K1 | — | P3^0 |
| 按键 K2 | — | P3^1 |
| 按键 K3 | — | P3^2 |
| 按键 K4 | — | P3^3 |

> 📷 接线图待补充（实物照片拍摄后更新）。仿真可直接打开 `proteus/基于LCD1602的万年历.DSN` 对照。

## 🛠️ 快速上手

### 编译与烧录（实物）

> 仓库**不提交** `.uvproj`，请用源码新建工程。

1. 用 Keil4 **新建** C51 工程，芯片选 STC89C52  
2. 将 [`c/main.c`](./c/main.c) 与 `c/STARTUP.A51` 加入工程并编译，生成 `.hex`  
3. 用 STC-ISP 连接开发板  
4. **先点击"下载"，再给开发板上电**（冷启动）  
5. 等待烧录完成，对照元数据表「预期现象」检查 LCD 显示  

### 仅仿真

1. Proteus 打开 [`proteus/基于LCD1602的万年历.DSN`](./proteus/基于LCD1602的万年历.DSN)  
2. 加载编译出的 `.hex` 后运行  

### 功能操作

| 按键 | 正常模式 | 设置模式 |
|:---|:---|:---|
| K1 | 无操作 | 进入设置 / 退出并保存 |
| K2 | 无操作 | 切换设置项（秒→分→时→日→月→年） |
| K3 | 无操作 | 当前选项 +1 |
| K4 | 无操作 | 当前选项 -1 |

## 💡 代码亮点

### 1. DS18B20 状态机（非阻塞设计）

```c
// 不用 delay 等待转换完成，而是用状态机分步处理
switch(ds18b20_state) {
    case 0: // 启动转换
        DS18B20_StartConvert();
        ds18b20_state = 1;
        break;
    case 1: // 等待转换完成
        if(ds18b20_wait == 0)
            ds18b20_state = 2;
        break;
    case 2: // 读取温度
        temperature = DS18B20_ReadTemp();
        ds18b20_state = 0;
        break;
}
```

这样设计的好处：温度采集不阻塞主循环，按键响应不会卡顿。

### 2. DS1302 双重校验读取

```c
// 连续读取两次，完全一致才认为数据有效
DS1302_ReadBurst(buf1);
DS1302_ReadBurst(buf2);
for(i = 0; i < 8; i++) {
    if(buf1[i] != buf2[i]) break;
}
if(i == 8) break; // 两次一致，数据有效
```

DS1302在高速读取时可能出错，双重校验保证时间准确。

### 3. 蔡勒公式计算星期

```c
// 根据年月日计算星期几（0=周日，1=周一...）
unsigned char GetWeekday(unsigned char year, unsigned char month, unsigned char day) {
    // 蔡勒公式...
}
```

## 📝 踩坑记录

**坑1：LCD1602显示方块**
→ 对比度没调。板子上有个蓝色电位器，转一转就清晰了。

**坑2：DS1302时间不走**
→ 检查CH位（秒寄存器最高位）。CH=1表示时钟停止，需要写入0才能启动。

**坑3：温度显示负数时乱码**
→ 负数处理逻辑没考虑周全，需要单独处理符号位和绝对值。

**坑4：按键设置时间时LCD闪烁**
→ 设置模式下不应该每次都重写整个显示内容，只更新变化的部分。

## 📺 相关视频

> 视频制作中，完成后会更新链接。

---

[← 返回C52](../../README.md) · [← 返回首页](../../../README.md)
